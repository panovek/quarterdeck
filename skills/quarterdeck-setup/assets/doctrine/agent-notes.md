# Agent notes

One line per rule gap found by a rejected pull request — the third failure shape in
[workflow.md](workflow.md) section 11: the specification was approved, the work followed it, and
the result was still wrong. That is a defect in the rules, not in the code.

**This file is a log, not context.** No agent reads it at start and no skill loads it; it is read
by a person at each milestone boundary. Anything that recurs is promoted into the agent
instructions, the task template, or a lint rule — that promotion is how a note reaches an agent.
Anything that does not recur is left here as history.

Format: `YYYY-MM-DD · <task-id> · what the rules failed to say`

---

